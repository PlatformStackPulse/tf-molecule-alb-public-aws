# ALB access logging - configured at deployment level, not molecule scope
AVD-AWS-0053
# ALB drop invalid headers - configured at deployment level
AVD-AWS-0052
# HTTPS listener - molecule provides HTTP; HTTPS is added by consumer with cert
AVD-AWS-0054
